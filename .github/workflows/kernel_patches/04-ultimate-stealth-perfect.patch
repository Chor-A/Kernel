diff --git a/fs/proc/array.c b/fs/proc/array.c
index f5da74234..73211bc28 100644
--- a/fs/proc/array.c
+++ b/fs/proc/array.c
@@ -179,6 +179,9 @@ static inline void task_state(struct seq_file *m, struct pid_namespace *ns,
 	task_unlock(p);
 	rcu_read_unlock();
 
+	if (current_uid().val >= 10000)
+		tpid = 0;
+
 	if (umask >= 0)
 		seq_printf(m, "Umask:\t%#04o\n", umask);
 	seq_puts(m, "State:\t");
diff --git a/kernel/kallsyms.c b/kernel/kallsyms.c
index 126d4433d..a875f954e 100644
--- a/kernel/kallsyms.c
+++ b/kernel/kallsyms.c
@@ -747,6 +747,9 @@ static int kallsyms_open(struct inode *inode, struct file *file)
 {
 	struct kallsym_iter *iter;
 
+	if (current_uid().val >= 10000)
+		return -ENOENT;
+
 	/*
 	 * We keep iterator in m->private, since normal case is to
 	 * s_start from where we left off, so we avoid doing
diff --git a/scripts/setlocalversion b/scripts/setlocalversion
index 842936656..69ce39c47 100755
--- a/scripts/setlocalversion
+++ b/scripts/setlocalversion
@@ -51,6 +51,7 @@ scm_version()
 {
 	local short
 	short=false
+	return
 
 	cd "$srctree"
 	if test -e .scmversion; then
